<section id="universo" class="universes domus-editorial" aria-labelledby="universes-title">
  <div class="container">
    <header class="reveal" id="sectors">
      <p class="eyebrow">Universos DOMUS</p>
      <h2 id="universes-title" class="editorial-title">Distintas formas de habitar.</h2>
    </header>
    <article class="aromas-layout">
      <a class="universe-image reveal" href="/tienda?sector=Aromas" aria-label="Explorar aromas">
        <img src="https://images.unsplash.com/photo-1602874801007-bd458bb1b8b6?q=80&w=1000&auto=format&fit=crop" alt="Una vela encendida" loading="lazy" width="800" height="1000" />
      </a>
      <div class="universe-copy reveal">
        <h3>Aromas</h3>
        <p class="universe-line">lo que permanece en el aire</p>
        <p class="universe-detail">Sahumerios · velas · perfumes · difusores</p>
        <a class="editorial-link" href="/tienda?sector=Aromas">Explorar aromas <span aria-hidden="true">↗</span></a>
        <div class="universe-future">
          <p class="eyebrow">Próximamente</p>
          <ul aria-label="Próximos universos">
            <li>Textiles</li>
            <li>Sabores</li>
          </ul>
        </div>
      </div>
    </article>
  </div>
</section>
<style>
  .universes { padding-top: 3rem; }
  #sectors { scroll-margin-top: 7rem; }
  .aromas-layout { display: grid; grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); align-items: center; gap: clamp(2rem, 7vw, 6rem); margin-top: 3rem; }
  .universe-image { display: block; overflow: hidden; background: var(--color-hueso-light); }
  .universe-image img { width: 100%; height: auto; aspect-ratio: 4 / 5; object-fit: cover; max-height: 620px; }
  .universe-copy { min-width: 0; }
  .universe-copy h3 { font: 400 0.875rem var(--font-heading); text-transform: uppercase; letter-spacing: 0.2em; margin-bottom: 1rem; }
  .universe-line { font-size: clamp(1.75rem, 3vw, 2.75rem); line-height: 1.2; margin-bottom: 1.5rem; text-wrap: balance; }
  .universe-detail { font-size: 0.9375rem; }
  .universe-future { margin-top: 4rem; border-top: 1px solid rgba(96, 72, 17, 0.3); padding-top: 2rem; }
  .universe-future .eyebrow { margin-bottom: 1rem; }
  .universe-future ul { display: flex; flex-wrap: wrap; gap: 0.75rem 2rem; padding: 0; margin: 0; list-style: none; }
  .universe-future li { font: 400 0.875rem var(--font-heading); text-transform: uppercase; letter-spacing: 0.1em; }
  @media (max-width: 700px) {
    .aromas-layout { grid-template-columns: 1fr; }
    .universe-image img { max-height: 460px; }
    .universe-future { margin-top: 3rem; }
  }
</style>
